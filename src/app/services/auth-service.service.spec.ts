import { AuthServiceService } from './auth-service.service';

describe('AuthServiceService', () => {
  it('should create an instance', () => {
    expect(new AuthServiceService()).toBeTruthy();
  });
});
